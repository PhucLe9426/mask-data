# Local LLM (vLLM chạy trên VM 111)
LOCAL_LLM_URL=http://192.168.210.221:8000/v1/chat/completions
LOCAL_LLM_MODEL=Qwen/Qwen2.5-3B-Instruct

# Public LLM — điền khi đã chọn nhà cung cấp
PUBLIC_LLM_URL=
PUBLIC_LLM_API_KEY=
PUBLIC_LLM_MODEL=

# Timeout gọi API (giây)
REQUEST_TIMEOUT=180

# Thời gian giữ session mapping trong bộ nhớ (giây), mặc định 1 giờ
SESSION_TTL_SECONDS=3600

# PostgreSQL lưu lịch sử hội thoại
DATABASE_URL=postgresql://masking:masking_dev_password@127.0.0.1:5434/masking_app
DATABASE_MIN_POOL_SIZE=1
DATABASE_MAX_POOL_SIZE=5

# Đăng nhập: bật AUTH_COOKIE_SECURE=true khi chạy production qua HTTPS
AUTH_SESSION_DAYS=7
AUTH_COOKIE_SECURE=false
AUTH_COOKIE_NAME=masking_session

# pgAdmin chạy bằng Docker tại http://127.0.0.1:5051
PGADMIN_PORT=5051
PGADMIN_DEFAULT_EMAIL=admin@masking-app.com
PGADMIN_DEFAULT_PASSWORD=pgadmin_dev_password

# Upload file: tối đa 10 MB và 200.000 ký tự sau khi trích xuất
MAX_UPLOAD_BYTES=10485760
MAX_EXTRACTED_CHARS=200000

# Kích thước mỗi đoạn gửi tuần tự tới local LLM để phát hiện entity
LOCAL_LLM_CHUNK_CHARS=12000
